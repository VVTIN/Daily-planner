import 'package:flutter/material.dart';

class TSizes {
  static double spaceBtwItem = 16.0;
  static double defaultSpace = 24.0;
  static double spaceBtwSection = 32.0;

  static double spacing = 64.0;

  static EdgeInsets buffer = EdgeInsets.only(
    top: spacing,
    bottom: defaultSpace,
    left: defaultSpace,
    right: defaultSpace,
  );
}
